# Contexto del agente IA y notas internas (no son parte del repo público)
context.md
tasks.json
docs/

# Binarios y artefactos de build
/bin/
/dist/
/chainview
/cmd/chainview/chainview

# Tests y cobertura
*.out
coverage.out
coverage.html

# Secretos / API keys (Alchemy, Infura, Etherscan) y config local
.env
.env.*
config.local.toml
*.local.toml

# Go workspace (local, no se versiona)
go.work
go.work.sum

# Sistema operativo y editores
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
